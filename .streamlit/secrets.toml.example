# Copy this file to .streamlit/secrets.toml and fill in your key.
# .streamlit/secrets.toml is gitignored — never commit it.
# On Streamlit Community Cloud, set secrets via the app dashboard instead.

GEMINI_API_KEY = "AIza..."
PASSWORD = "your-password-here"
